overwrite: true
schema: "http://localhost:5555/graphql"
generates:
  src/__generated__/graphql.ts:
    config:
      scalars:
        Void: "void"
    plugins:
      - "typescript"
      - "typescript-resolvers"
